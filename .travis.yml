sudo: false
language: cpp
compiler:
  - clang
  - gcc
os:
- linux
- osx
dist: trusty
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-trusty-4.0
    packages:
    - gcc-7
    - clang-4.0
